# Session Log — 2026-04-21
## Scope
- Fixed stale transcript recall behavior after confirming Krisp ingest itself was healthy.
- Added Krisp verification/replay tooling and updated operator docs to match the live n8n-volume audit path.
- Deployed the recall fix to production and verified fresh April 20 transcript results through the live memory webhook.

## What Changed
### 1) Infrastructure / Runtime
- Deployed `main` at commit `6cf21e7` to production via the VM GitOps path.
- Rebuilt and restarted `memory-api` and `openclaw` so the new recall logic was live.
- Files touched:
  - `automation/memory-api/app/recall_service.py`
  - `.github/workflows/ci.yml`
  - `scripts/verify.sh`
  - `scripts/verify-krisp-ingest.sh`

### 2) Workflow / Application
- Implemented transcript recency-aware recall for natural-language queries like `latest transcript summaries` and `today transcript summaries`.
- Added transcript-family dedupe so generic recency queries prefer one representative row per Krisp meeting family (`#key_points` > base transcript > `#action_items`).
- Added regression tests covering temporal query parsing, transcript-family ranking, and router behavior.
- Files touched:
  - `automation/memory-api/tests/test_recall_service.py`
  - `automation/memory-api/tests/test_router.py`
  - `scripts/replay-krisp-webhook.sh`

### 3) Operations / Docs
- Updated memory/Krisp runbooks and user docs to reference the real audit location inside the shared n8n volume at `/home/node/.n8n/krisp-ingest.ndjson`.
- Updated Openclaw/TARS guidance so natural-language recall defaults to `./bin/recall-unified`, with `./bin/recall-memory` kept for exact memory-only lookups.
- Added `--fixture NAME` support to `scripts/replay-krisp-webhook.sh` using governed Krisp fixtures from `workflows/governed/mem-06-krisp-webhook-adapter/test-fixtures.json`.

## Deployment Notes
- Deploy method used: gitops
- VM apply performed: yes

## Validation Checks Passed
- `python3 -m unittest discover -s automation/memory-api/tests -p 'test_*.py'`
- `shellcheck scripts/replay-krisp-webhook.sh scripts/verify.sh scripts/verify-krisp-ingest.sh`
- `python3 -m py_compile automation/memory-api/app/recall_service.py automation/memory-api/tests/test_recall_service.py automation/memory-api/tests/test_router.py`
- `./scripts/verify.sh`
- Production live recall check: `latest transcript summaries` returned fresh April 20, 2026 transcript rows instead of stale March-only results.
- External checks passed:
  - `https://n8n.satoic.com` -> 200
  - `https://openclaw.satoic.com` -> 200
  - `https://portainer.satoic.com` -> 401

## Backups Taken
- Config backup: no
- DB volume backup: no

## Known Risks / Notes
- Session closeout `git fetch` hit a local sandbox restriction on `.git/FETCH_HEAD`, but branch parity still showed local `main` matched `origin/main` at `6cf21e7`.
- The GitHub Actions `Deploy to Production` workflow also queued after the push; direct VM GitOps deploy was used to make the fix live immediately.
- Untracked local paths remained outside this change set: `ops/dr-manifests/dr-backup-2026-03-29-215612.md`, `output/`, and `tmp/`.
- Secrets rotated (if exposed): n/a

## Rollback / Recovery
- Revert commit `6cf21e7` and redeploy `main` through the standard GitOps path if transcript recall regresses.
- Restart/inspect first:
  - `memory-api`
  - `openclaw`
  - `n8n-webhook`
- First checks:
  - `./scripts/verify.sh`
  - `./scripts/verify-krisp-ingest.sh`
  - `docker exec automation-n8n-webhook-1 sh -lc 'tail -n 20 /home/node/.n8n/krisp-ingest.ndjson'`

## Next Session Plan
1. Queue live Krisp transcripts and confirm TARS/Openclaw returns them correctly through `./bin/recall-unified`.
2. Replay governed Krisp fixtures in dev and confirm the new verification/docs flow is usable there end to end.
3. Watch for any remaining narrow transcript phrasings that still need ranking or recency tuning.
