# Session Log — 2026-03-13

## Scope
- Implement the first production-ready content registry subsystem.
- Replace TAR's ad hoc link-capture prototype with governed workflows.
- Validate registry capture, processing, query, list, and review end to end.

## What Changed
### 1) Registry Schema / API
- Added the new `registry` Postgres schema and migration:
  - `sql/014_registry_schema.sql`
- Added registry API support inside `memory-api`:
  - `automation/memory-api/app/registry_service.py`
  - `automation/memory-api/app/routes/registry.py`
  - `automation/memory-api/app/routes/health.py`
  - `automation/memory-api/app/main.py`
- Added runtime config for archive path and fetch limits:
  - `automation/memory-api/app/config.py`
  - `automation/docker-compose.yml`
  - `automation/.env.example`
- Added the local archive mount and repo hygiene for archived source material:
  - `automation/registry-archive/README.md`
  - `.gitignore`

### 2) Governed Registry Workflow Family
- Added the governed registry workflow pack:
  - `workflows/governed/reg-01-registry-capture/**`
  - `workflows/governed/reg-02-registry-processor/**`
  - `workflows/governed/reg-03-registry-query/**`
  - `workflows/governed/reg-04-registry-list-review/**`
- Added the shared n8n credential template:
  - `workflows/governed/_shared/registry-webhook-header-auth.credential.template.json`
- Added operator-facing registry contract docs:
  - `ops/registry-external-interfaces.md`

### 3) Production Cutover
- Applied the `registry` schema in production Postgres.
- Generated and imported a production `Registry Webhook Header Auth` credential in n8n.
- Imported the governed `REG-01..REG-04` workflows into production n8n.
- Exported TAR's active prototype workflow for local reference:
  - `/tmp/link-registry-capture-prototype-2026-03-13.json`
- Disabled all four prototype `Link Registry - Capture (private webhook)` workflows.
- Activated the governed registry workflows and forced webhook registration through the n8n CLI.

### 4) Registry Runtime Fixes During Validation
- Fixed metadata parsing when asyncpg returns JSONB columns as strings.
- Fixed registry list response formatting so `metadata` and `captures` are decoded correctly.
- Fixed external fetch processing by relaxing TLS verification only for registry source-fetch calls after production container verification failures on normal web pages.

## Deployment Notes
- Deploy method used: GitOps
- VM apply performed: yes

## Validation Checks Passed
- Repo validations:
  - `python3 -m py_compile automation/memory-api/app/config.py automation/memory-api/app/main.py automation/memory-api/app/routes/health.py automation/memory-api/app/routes/registry.py automation/memory-api/app/registry_service.py`
  - `find workflows/governed/reg-* -name '*.json' -print0 | xargs -0 -n1 jq empty`
  - `docker compose --env-file automation/.env.example -f automation/docker-compose.yml config`
- Production health:
  - `memory-api /health` now reports `schema.memory=ready` and `schema.registry=ready`
- Production registry smoke:
  - `POST /webhook/registry/capture` -> `200`, returns `item_id`, `processing_status=captured`, `review_state=inbox`
  - background processing converges the canonical item to `processing_status=ready`
  - `POST /webhook/registry/query` returns the processed item
  - `POST /webhook/registry/list` returns the structured inbox item
  - `POST /webhook/registry/review` updates `review_state` successfully
- Production smoke example:
  - canonical item id: `01d7985f-e33a-41ce-b186-bf0bc9b6b088`
  - canonical URL: `https://example.com/`
  - title: `Example Domain`
  - archive path: `/var/lib/registry-archive/web/example.com/01d7985f-e33a-41ce-b186-bf0bc9b6b088.md`
  - capture history deduped into one canonical item with multiple `registry.captures` rows

## Known Risks / Notes
- The current live registry ingress is `https://n8n.satoic.com/webhook/registry` plus `x-registry-webhook-secret`; this is secret-protected public ingress, not true Tailnet-only capture yet.
- Registry source fetches now use relaxed TLS verification for external content-fetch calls only. This was required because production `memory-api` could not verify standard web certificates during fetch processing.
- The local Mac stack still has a DB credential mismatch for `memory-api`, so local end-to-end runtime verification was limited; production validation completed successfully.

## Rollback / Recovery
- Registry rollback path:
  1. deactivate `REG-01..REG-04`
  2. keep TAR's prototype workflows disabled unless an operator intentionally re-enables them
  3. registry schema and rows can remain in place even if the n8n ingress is disabled
- Reference export of TAR's prototype workflow:
  - `/tmp/link-registry-capture-prototype-2026-03-13.json`

## Next Session Plan
1. Configure the iPhone Shortcut against the live registry capture endpoint and test real saves from Safari / X / YouTube / TikTok.
2. Decide whether to keep secret-protected public ingress or add a true Tailnet-private registry front door.
3. Add TARS-side prompt/tool guidance so saved-content queries prefer `REG-03` and inbox curation uses `REG-04`.
