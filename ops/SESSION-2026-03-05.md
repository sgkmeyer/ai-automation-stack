# Session Log — 2026-03-05

## Scope
- Apply security/process hardening recommendations from infrastructure audit.
- Reconcile runbooks and status notes with current production behavior.

## What Changed
### 1) Infrastructure / Runtime
- Hardened n8n runtime exposure by disabling environment-variable access in code nodes for `n8n`, `n8n-worker`, and `n8n-webhook`.
- Pinned n8n image from floating `latest` to `n8nio/n8n:2.9.2` across runtime services.
- Upgraded Openclaw pin from `2026.3.1` to `2026.3.2` in `automation/openclaw-image/Dockerfile` (verified from npm `latest` dist-tag).
- Files touched:
  - `automation/docker-compose.yml`
  - `automation/openclaw-image/Dockerfile`

### 2) Workflow / Application
- Hardened deploy pipelines to enforce SSH host key checking in both prod and dev GitHub Actions workflows.
- Added required secret usage for host key pinning: `VM_SSH_KNOWN_HOSTS`.
- Files touched:
  - `.github/workflows/deploy-prod.yml`
  - `.github/workflows/deploy-dev.yml`

### 3) Operations / Docs
- Added safety preview/confirmation to emergency rsync path (`sync-to-vm.sh` now dry-runs first before `--delete`).
- Updated VM guard script allowlist docs and service mapping (`n8n-webhook` + `n8n-task-runner` usage consistency).
- Updated backup commands to use `tar -h` where symlink following is required.
- Reconciled documentation drift:
  - Openclaw external expected status is now documented as `200` (gateway token auth).
  - `ops/today.md` updated with completed hardening items and new SSH host-key note.
  - Security audit doc updated to reflect post-fix dev status.
  - README latest session pointer and auth model updated.
- Files touched:
  - `scripts/sync-to-vm.sh`
  - `scripts/vm-safe.sh`
  - `scripts/end-session.sh`
  - `ops/runbooks.md`
  - `ops/today.md`
  - `ops/security-audit-2026-03-03.md`
  - `README.md`

## Deployment Notes
- Deploy method used: none
- VM apply performed: no

## Validation Checks Passed
- `shellcheck scripts/*.sh` passed.
- `docker compose ... config` validation passed (with expected unset-env warnings under `/dev/null` env-file).
- GitHub Actions `Deploy to Dev` green after CI workflow resilience fixes (host secret sanitation/fallback + dev Caddy scale=0).
- GitHub Actions `Deploy to Production` green with successful smoke test after `main` push.

## Backups Taken
- Config backup: no
- DB volume backup: no

## Known Risks / Notes
- CI deploy jobs now require `VM_SSH_KNOWN_HOSTS` to be present and kept current on host-key rotation.
- Additional floating image tags still exist in non-n8n services and should be pinned in a controlled follow-up.
- `tailscale/github-action` authkey mode is still in use and emits deprecation warning; OAuth client migration remains open.
- Secrets rotated (if exposed): n/a

## Rollback / Recovery
- Revert this session’s config/docs commit and redeploy if needed.
- If n8n code nodes require env access for legacy workflows, restore `CODE_ENABLE_PROCESS_ENV_ACCESS=true` and redeploy.
- For CI deploy failures after this change, verify `VM_SSH_KNOWN_HOSTS` content against current VM host key.

## Next Session Plan
1. Migrate `tailscale/github-action` from authkey mode to OAuth client credentials.
2. Pin remaining floating container tags (Chromium/Openclaw base/runners) with a tested version matrix.
3. Implement Caddy bind-mount change handling in deploy path (`restart caddy` when Caddyfile changes).
