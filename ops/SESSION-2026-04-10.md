# Session Log — 2026-04-10

## Scope
- Triage TARS's semantic-memory failure report in OpenClaw.
- Upgrade OpenClaw from `2026.3.24` to `2026.4.9`.
- Stabilize dev and production GitOps deploys after the upgrade.
- Implement the first hybrid memory stack slice with a wiki lane and agent-neutral write contracts.

## What Changed
### 1) Infrastructure / Runtime
- Bumped the pinned OpenClaw image version:
  - `ARG OPENCLAW_VERSION=2026.3.24` -> `2026.4.9`
- Updated the image build to force-install the pinned CLI over the upstream base image copy:
  - `npm i -g "openclaw@${OPENCLAW_VERSION}"` -> `npm i -g --force "openclaw@${OPENCLAW_VERSION}"`
- Fixed the workspace helper wrapper:
  - `automation/openclaw/workspace/bin/remember` now resolves `bin/memory` relative to its own script directory instead of relying on the caller's working directory.
- Fixed both GitOps deploy scripts so OpenClaw's mounted runtime directory is handed back to UID `1000:1000` before `docker compose up`, not only after deploy:
  - `scripts/gitops-deploy.sh`
  - `scripts/gitops-deploy-dev.sh`
- Redeployed dev and production to the new image and deploy-script behavior.

### 2) Memory Triage
- Confirmed the reported issue was real on production before the upgrade:
  - `automation-openclaw-1` logs contained `[memory] sync failed (search): Error: database is not open`
- Confirmed this was a partial degradation:
  - shared routed recall via `./bin/recall-unified` still returned transcript-backed results
  - the broken lane was OpenClaw's built-in semantic memory sync/search backend
- After the `2026.4.9` rollout and fresh container recreation:
  - `openclaw memory status --json` on both dev and production reports the builtin backend healthy
  - vector search extension is available
  - no fresh `database is not open` or embedding/provider errors appeared in post-upgrade logs

### 3) Operations / Docs
- Added this dated session log.
- Updated [`ops/today.md`](/Users/sgkmeyer/ai-automation-stack/ops/today.md) for the new OpenClaw version, memory-state notes, and deploy-script fix.

### 4) Hybrid Memory Stack Phase 1
- Added a new `memory-api` wiki subsystem:
  - file-backed wiki search/read
  - wiki proposal queue
  - review/approval flow
  - wiki lint/health endpoints
- Extended the unified recall router with a `wiki` lane for synthesis-oriented questions:
  - broad synthesis now routes wiki-first
  - live lanes remain the fallback path when coverage is weak or missing
- Standardized an agent-neutral actor envelope across write-capable routes:
  - `actor_type`
  - `actor_id`
  - `session_id`
  - `source_client`
  - optional `reason`
- Updated memory, context, ingest, and registry request flows to carry actor metadata for audit/debugging.
- Added direct client-facing wiki CLI/scripts:
  - `scripts/wiki-api.sh`
  - `scripts/bootstrap-obsidian-wiki.sh`
  - `scripts/sync-wiki-outbox.sh`
- Added OpenClaw wiki wrappers and policy/docs:
  - `automation/openclaw/workspace/bin/wiki`
  - `automation/openclaw/workspace/bin/query-wiki`
  - `automation/openclaw/workspace/WIKI_POLICY.md`
  - updated `AGENTS.md` and `TOOLS.md`
- Updated governed n8n workflow JSON so webhook-driven memory/registry writes preserve actor metadata instead of dropping it in transit.
- Added compose support for mounted wiki vault/proposal directories:
  - `automation/wiki-vault`
  - `automation/wiki-proposals`
- Fixed the first live wiki write-path bug after TAR's smoke test:
  - `POST /wiki/proposals` was 500ing because `_serialize_frontmatter()` attempted to test an empty list inside a set membership expression
  - `automation/memory-api/app/wiki_service.py` now handles list-valued frontmatter fields before scalar emptiness checks
  - deployed the hotfix on both dev and production as commit `10ef2d8`

## Deployment Notes
- Deploy method used: gitops
- VM apply performed: yes
- Dev deploy needed one extra pass because a stale dev container name blocked `memory-api` recreation during the first rerun.

## Validation Checks Passed
- `npm view openclaw version` returned `2026.4.9`
- Dev validation:
  - `automation-dev-openclaw-1` healthy
  - `openclaw --version` returned `OpenClaw 2026.4.9 (0512059)`
  - `openclaw agent ... "dev-system-check-ok"` succeeded
  - `openclaw memory status --json` returned healthy builtin/vector status
- Production validation:
  - `automation-openclaw-1` healthy
  - `openclaw --version` returned `OpenClaw 2026.4.9 (0512059)`
  - `openclaw agent ... "system-check-ok"` succeeded on `openai-codex/gpt-5.4`
  - `openclaw memory status --json` returned healthy builtin/vector status
  - `./scripts/vm-safe.sh --yes health` passed after rollout
  - internal Caddy smoke checks returned redirect responses for both `openclaw.satoic.com` and `n8n.satoic.com`
- Hybrid memory implementation validation:
  - `python3 -m py_compile automation/memory-api/app/*.py automation/memory-api/app/routes/*.py`
  - `bash -n` passed for the updated/new memory, registry, wiki, and OpenClaw wrapper scripts
  - modified governed workflow JSON files parsed successfully
  - live dev/prod wiki checks now passed for:
    - `./bin/wiki --help`
    - `./bin/wiki health`
    - `./bin/query-wiki "hybrid memory" --limit 3`
    - `./bin/wiki proposals --status pending_review`
    - `./bin/wiki lint --limit 10`
    - `./bin/wiki propose ...`
    - `./bin/wiki review --proposal-id ... --action approve`
    - `./bin/wiki proposals --status approved`
  - live routing check passed:
    - `./bin/recall-unified --query "Give me the deeper picture on the hybrid memory stack and how it has evolved"`
    - returned `intent_type=broad_synthesis`, `primary_lane=wiki`, `fallback_used=true`
  - actor metadata was visible in live proposal records and review records (`actor_type=tars`, `actor_id=openclaw`, `source_client=openclaw`)

## Backups Taken
- Config backup: no new dedicated archive taken during this session
- DB volume backup: no
- OpenClaw runtime auto-backup created during config overwrite:
  - `/home/node/.openclaw/config.json.bak`

## Known Risks / Notes
- The repo-tracked `automation/openclaw/config.json` still shows the older default model (`gpt-5.2`), while the live production runtime hot-reloaded and retained `openai-codex/gpt-5.4`. Preserve runtime config on future deploys unless/until the repo copy is intentionally reconciled.
- Direct `openclaw memory search` is healthy again, but the builtin memory index only contains the small `memory/` corpus, so transcript recall still depends on the separate unified/router lane for broader historical recall.
- The wiki lane is intentionally review-gated in Phase 1. Approved proposals land in a VPS outbox and still need sync-back into the Mac-canonical vault.
- The live wiki vault still has `page_count=0`; approved proposals currently populate the VPS outbox, not the canonical vault, until the sync-back step is run.
- `MEMORY.md` is now treated as a thin boot brief conceptually, but there is still runtime/local drift risk anywhere agents rely on old expectations about it as a long-term summary surface.
- Local `vm-safe.sh check-external` from Codex desktop was DNS-limited in this session, so external verification was substituted with server-side Caddy checks plus live agent turns.

## Rollback / Recovery
- If `2026.4.9` regresses:
  - revert the Dockerfile pin to `2026.3.24`
  - keep the deploy-script ownership fix
  - redeploy dev first, then production
- First place to look:
  - `docker logs --since 15m automation-openclaw-1`
  - `docker logs --since 15m automation-dev-openclaw-1`
  - `openclaw memory status --json`
  - `openclaw agent --to +15555550123 --message "system-check-ok" --json`

## Next Session Plan
1. Decide whether to reconcile the repo copy of `automation/openclaw/config.json` with the live `gpt-5.4` default or continue preserving runtime drift intentionally.
2. Watch for recurrence of the builtin memory-db failure now that both stacks have been recreated on `2026.4.9`.
3. If memory instability returns, capture `openclaw memory status --json --deep` and the first failing search log window before restarting anything.
4. Run the Mac-side sync/review loop so approved VPS outbox pages can be promoted into the canonical Obsidian vault and rechecked through `query-wiki`.
