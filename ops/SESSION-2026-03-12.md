# Session Log — 2026-03-12

## Scope
- Tightened the repo operating contract and production verification path.
- Rotated the remaining production credentials and SSH keys.
- Implemented and activated Krisp webhook ingress for transcript capture into the second brain.

## What Changed
### 1) Infrastructure / Runtime
- Added and tightened operational guardrails:
  - `ops/OPERATING-CONTRACT.md`
  - `scripts/verify.sh`
  - `scripts/vm-safe.sh`
  - `scripts/verify-memory-webhook.sh`
- Extended production verification to cover the live memory webhook path.
- Cleaned local repo hygiene with `.gitignore` updates for machine-local noise.

### 2) Security / Credentials
- Rotated `satoic_ci` and updated GitHub secret `VM_SSH_PRIVATE_KEY`.
- Rotated `satoic_operator` on the Mac and VM.
- Rotated `POSTGRES_PASSWORD` to a new URL-safe value and resynced DB-dependent services.
- Captured the operational constraint that `POSTGRES_PASSWORD` must remain URL-safe with the current DSN assembly.

### 3) Krisp Ingest
- Added `MEM-06 - Krisp Webhook Adapter` and governed workflow artifacts:
  - `workflows/governed/mem-06-krisp-webhook-adapter/**`
- Added Krisp replay helper:
  - `scripts/replay-krisp-webhook.sh`
- Imported and activated `MEM-06` in dev and production with the `Krisp Webhook Header Auth` credential.
- Validated dev replay against the queue-mode `n8n-webhook` service.
- Fixed the production payload mapping after inspecting live Krisp webhook executions:
  - actual transcript events arrived as `transcript_created`
  - meeting identity nested under `data.meeting.*`
  - transcript text nested under `data.content.description`
- Re-deployed production and confirmed the first real meeting ingested successfully:
  - title: `Stephan / Dana`
  - source_ref: `krisp:019ce2c77998759e9e3d93b1baf7c19b`
- Fixed event-family storage for Krisp follow-on payloads:
  - `transcript_created` stays on the base meeting `source_ref`
  - `key_points_generated` writes to `#key_points`
  - `action_items_generated` writes to `#action_items`
  - explicit action-item rows are now created under `#action_items#action_item:N`
- Reduced Openclaw recall latency:
  - production Openclaw memory calls now target internal `n8n-webhook` instead of the public HTTPS route
  - `memory-api /recall` now ranks Krisp key points and action-item entries above the raw transcript row

## Deployment Notes
- Deploy method used: GitOps
- VM apply performed: yes

## Validation Checks Passed
- Production public smoke:
  - `https://n8n.satoic.com` -> 200
  - `https://openclaw.satoic.com` -> 200
  - `https://portainer.satoic.com` -> 401
- Production memory webhook smoke passed end to end.
- `memory-api` health passed in production.
- Dev Krisp adapter replay passed for:
  - transcript-bearing payload
  - ignored non-transcript payload
  - duplicate transcript delivery behavior
  - invalid secret rejection
- Production Krisp adapter validation passed for:
  - public endpoint/auth reachability
  - nested `transcript_created` payload replay
  - first real Krisp meeting ingest
- Production Krisp event-family validation passed for:
  - `Jennifer / Stephan` transcript row preserved
  - separate `#key_points` summary row created
  - separate `#action_items` summary row created
  - 5 explicit `action_item` rows created without duplicate amplification on resend

## Backups Taken
- Config backup: no
- DB volume backup: no

## Known Risks / Notes
- Obsidian ingress is still manual `./scripts/sync-obsidian-vault.sh`; no schedule yet.
- Krisp sample-note deliveries do not prove transcript ingest; only real `transcript_created` payloads do.
- Some Krisp events contain notes/key points without transcript text and are intentionally ignored in v1.
- `POSTGRES_PASSWORD` must remain URL-safe unless the compose-built DSNs are changed to URL-encode credentials.

## Rollback / Recovery
- Krisp rollback:
  - disable `MEM-06 - Krisp Webhook Adapter`
  - remove Krisp webhook configuration in the Krisp app
  - use manual transcript ingest via `scripts/memory-webhook.sh transcript ...`
- For adapter failures:
  - inspect `/home/node/.n8n/krisp-ingest.ndjson`
  - inspect n8n executions for `mem06krispwebhookadapter`
  - confirm `Memory API Header Auth` and `Krisp Webhook Header Auth`

## Next Session Plan
1. Run recall/UAT against the newly ingested Krisp meeting through Openclaw.
2. Generate end-user testing scripts for Openclaw, Obsidian, and Krisp.
3. Reduce the remaining manual operational path for Obsidian ingress.
