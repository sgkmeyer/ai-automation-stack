# Session Log — 2026-05-18

## Scope
- Opened a new operational session thoroughly: startup checks, repo state, VM health, live-state review, and smoke validation.
- Started Openclaw upgrade planning and implementation for latest stable `openclaw@2026.5.18`.
- Upgraded production Openclaw's default OpenAI Codex model from `openai-codex/gpt-5.4` to `openai-codex/gpt-5.5`.
- Simplified the dev/prod deploy process around `vm-safe.sh` as the canonical production deploy surface.

## What Changed
### 1) Infrastructure / Runtime
- Updated the Openclaw image pin in `automation/openclaw-image/Dockerfile` from `2026.4.26` to `2026.5.18`.
- Updated production runtime Openclaw model default to `openai-codex/gpt-5.5` with `openclaw models set`.
- Converted production GitHub Actions from push-triggered deploy to manual production smoke only.
- Expanded dev GitHub Actions smoke to validate dev n8n, Openclaw health/version, and memory-api health.
- Full session opener confirmed Tailscale connectivity and production compose status.
- Production services reported up: caddy, chromium, db, memory-api, n8n, n8n-task-runner, n8n-webhook, n8n-worker, openclaw, portainer, redis, toolbox.

### 2) Workflow / Application
- No workflow or application changes made.
- `MEM-05 - Transcript Ingest` and `MEM-06 - Krisp Webhook Adapter` were verified active by the smoke script.

### 3) Operations / Docs
- Created this dated session log.
- Reviewed `ops/today.md`, `ops/OPERATING-CONTRACT.md`, and session-opening scripts.
- Documented `vm-safe.sh deploy` as the production deploy surface in README, CLAUDE, runbooks, operating contract, and session opener.
- Split routine deploy health from Krisp freshness: `scripts/verify.sh` now warns, rather than fails, when recent Krisp ingestion activity is absent.
- Removed local generated/swap artifacts from the working tree and ignored generated `output/` and `tmp/` directories.
- Preserved and committed the validated 2026-03-29 DR manifest and 2026-04-21 session log.
- Noted existing working-tree drift before this session:
  - Modified: `ops/runbooks.md`
  - Untracked: `ops/.SESSION-2026-04-21.md.swp`, `ops/SESSION-2026-04-21.md`, `ops/dr-manifests/dr-backup-2026-03-29-215612.md`, `output/`, `scripts/openclaw-control-ui.sh`, `tmp/`

## Deployment Notes
- Deploy method used: dev GitHub Actions GitOps, then production `./scripts/vm-safe.sh --yes deploy`
- VM apply performed: yes
- Dev was fast-forwarded to `bace011` and deployed by `.github/workflows/deploy-dev.yml`.
- Production `main` was fast-forwarded to `bace011`; the GitHub production workflow remained at its environment approval gate, so production was applied with the repo's VM-safe GitOps wrapper.
- Model default change was applied live inside the production Openclaw container and persisted to `/home/node/.openclaw/config.json`.

## Validation Checks Passed
- `./scripts/start-session.sh --full`
  - Tailscale connected.
  - `git fetch --prune` completed.
  - VM compose status returned all expected services up; db, redis, memory-api, and openclaw showed healthy.
  - Start status: `CLEAN`.
- `./scripts/verify.sh`
  - VM compose status check passed.
  - External endpoint checks passed:
    - `https://n8n.satoic.com` -> 200
    - `https://openclaw.satoic.com` -> 200
    - `https://portainer.satoic.com` -> 401, expected pre-auth response
  - Memory webhook smoke passed: context set/get/delete.
  - Krisp workflow active check passed.
- `npm view openclaw version dist-tags --json`
  - Latest stable was `2026.5.18`; beta was `2026.5.19-beta.1`.
- Local checks before deploy:
  - `git diff --check` passed.
  - Production and dev `docker compose config --quiet` passed with pre-existing local env warnings.
  - Secret grep found only known placeholders/references.
- Dev deployment:
  - GitHub Actions run `26071229920` passed deploy and smoke jobs.
  - `automation-dev-openclaw-1` reported healthy.
  - `node --version` returned `v24.14.0`.
  - `openclaw --version` returned `OpenClaw 2026.5.18 (50a2481)`.
  - `openclaw memory status --json` returned healthy builtin/vector status.
  - `./bin/wiki health` returned `status=ready`.
  - `./bin/recall-unified --query "hybrid memory stack" --limit 2` returned a structured router response.
  - `/healthz` returned `200 {"ok":true,"status":"live"}`.
  - Agent smoke returned `dev-openclaw-upgrade-ok`.
- Production deployment:
  - `./scripts/vm-safe.sh --yes deploy` fast-forwarded the VM to `bace011`, rebuilt `satoic/openclaw-cap:latest`, and recreated `automation-openclaw-1`.
  - `automation-openclaw-1` reported healthy.
  - `node --version` returned `v24.14.0`.
  - `openclaw --version` returned `OpenClaw 2026.5.18 (50a2481)`.
  - `openclaw memory status --json` returned healthy builtin/vector status.
  - `openclaw models status --json` preserved production default `openai-codex/gpt-5.4`.
  - `./bin/wiki health` returned `status=ready`.
  - `./bin/recall-unified --query "Give me the deeper picture on the hybrid memory stack" --limit 2` returned wiki-lane results without fallback.
  - `/healthz` returned `200 {"ok":true,"status":"live"}`.
  - Agent smoke returned `prod-openclaw-upgrade-ok`.
- Model upgrade:
  - Official OpenAI docs listed GPT-5.5 as the latest flagship model for complex reasoning and coding.
  - `openclaw models list --json` showed `codex/gpt-5.5` available but unauthenticated for the local `codex` provider.
  - Dev one-off smoke with `--model codex/gpt-5.5` failed as expected because provider `codex` has no API key/auth.
  - Dev one-off smoke with `--model openai-codex/gpt-5.5` succeeded and returned `dev-openai-codex-55-ok`.
  - Production config backup was taken at `/home/node/.openclaw/config.json.pre-model-55-20260518.bak`; Openclaw also wrote `/home/node/.openclaw/config.json.bak`.
  - `openclaw models set openai-codex/gpt-5.5` updated the production default.
  - `openclaw models status --json` reported `defaultModel=openai-codex/gpt-5.5`.
  - Production default agent smoke without `--model` returned `prod-default-55-ok` and metadata showed `provider=openai-codex`, `model=gpt-5.5`.

## Backups Taken
- Config backup: yes, DR archive `/home/ubuntu/automation-full-2026-05-18-222948.tar.gz`
- DB volume backup: yes, DR archive `/home/ubuntu/automation-db-2026-05-18-222948.tar.gz`
- Local manifest: `ops/dr-manifests/dr-backup-2026-05-18-222948.md`

## Process Cleanup
- Production GitHub Actions deploy path converted to manual-only production smoke; `main` pushes should no longer create production deploy approval queues.
- Dev GitHub Actions smoke expanded beyond n8n to include dev compose status, Openclaw version/health, and memory-api health.
- `scripts/verify.sh` now treats Krisp recent-ingest as a warning-only freshness/activity signal; deploy health still blocks on VM compose, public endpoints, and memory webhook smoke.
- Local generated `output/` and `tmp/` paths removed and ignored.
- `scripts/openclaw-control-ui.sh`, `ops/SESSION-2026-04-21.md`, and `ops/dr-manifests/dr-backup-2026-03-29-215612.md` were validated as real artifacts for commit.
- Stale production GitHub runs canceled: 7 waiting push-triggered production runs.
- VM auto-stashes pruned: 58 older `gitops-deploy* auto-stash` entries dropped after DR backup; 4 same-day deploy stashes plus the named `manual-unstick` stash remain.
- Openclaw duplicate config noise pruned: 1,776 `config.json.clobbered.*` files removed after DR backup; named backups such as `config.json.pre-codex-auth-*`, `config.json.pre-model-55-20260518.bak`, `config.json.bak*`, and `config.json.last-good` were preserved.

## Known Risks / Notes
- Before the process cleanup, `./scripts/verify.sh` exited nonzero because `scripts/verify-krisp-ingest.sh` found no Krisp ingestion jobs in the default 72-hour lookback. This is now treated as a non-blocking freshness/activity assertion, not routine deploy health.
- Production logs after upgrade showed expected gateway startup, Telegram provider startup, and no Openclaw crash loop. Warnings observed: non-loopback bind warning and Telegram menu shortening due command payload size.
- Dev logs showed `Config observe anomaly: /home/node/.openclaw/config.json (missing-meta-vs-last-good)` once at startup, but health, memory, wrapper, and agent smokes passed.
- The repo-tracked Openclaw config copies are intentionally not source-of-truth for the live production runtime config; production preserved runtime config drift and now uses `openai-codex/gpt-5.5`.
- Pre-existing working-tree drift should be triaged before committing new work.
- Secrets rotated: n/a

## Rollback / Recovery
- No rollback needed; no runtime changes were applied.
- For Krisp verification follow-up, inspect recent `memory.ingestion_jobs` rows, n8n webhook logs, and `/home/node/.n8n/krisp-ingest.ndjson` inside the webhook container.

## Next Session Plan
1. Triage the pre-existing working-tree drift and decide what belongs in the next commit.
2. Decide whether the Krisp verifier should remain strict on 72-hour recency or separate wiring health from recent-ingest freshness.
3. Watch Openclaw logs for any recurrence of the dev-only config-observe anomaly or production model-auth warnings.
