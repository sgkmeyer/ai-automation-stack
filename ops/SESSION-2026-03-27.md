# Session Log — 2026-03-27

## Scope
- Restore production Openclaw after Codex OAuth refresh-token failure.
- Re-authenticate the `openai-codex` provider without re-running full setup.
- Verify the memory-query path that had been failing in Telegram.
- Upgrade Openclaw from `2026.3.8` to `2026.3.24`.

## What Changed
### 1) Infrastructure / Runtime
- Refreshed the production Openclaw Codex OAuth profile in-place via:
  - `openclaw models auth login --provider openai-codex`
- Avoided a full `openclaw onboard` run because production is on Openclaw `2026.3.8`, where the narrower model-auth flow works inside the container.
- Openclaw hot-reloaded the rewritten config after auth completed; no container restart was required.
- Bumped the pinned Openclaw version in the image build:
  - `ARG OPENCLAW_VERSION=2026.3.8` -> `2026.3.24`
- Deployed the `2026.3.24` image to both dev and production through the existing GitOps scripts.
- Runtime files touched on production:
  - `/home/node/.openclaw/config.json`
  - `/home/node/.openclaw/agents/main/agent/auth-profiles.json`

### 2) Workflow / Application
- Re-ran the failing style of request against live Openclaw:
  - `What are the 3 latest transcripts you can see from memory?`
- Confirmed the embedded agent completed successfully after the OAuth refresh instead of failing during token refresh.

### 3) Operations / Docs
- Added this dated session log.
- Updated [`ops/today.md`](/Users/sgkmeyer/ai-automation-stack/ops/today.md) to record the refreshed OAuth state and new last-updated date.
- Updated the Openclaw version pin and live-state notes for the `2026.3.24` upgrade.

## Deployment Notes
- Deploy method used: gitops
- VM apply performed: yes

## Validation Checks Passed
- Production logs before fix showed:
  - `refresh_token_reused`
  - `OAuth token refresh failed for openai-codex`
- `openclaw models status --json` after fix showed:
  - `openai-codex:default`
  - `status: ok`
  - fresh expiry timestamp
- Live production agent turn succeeded for the previously failing memory-style query.
- No new OAuth refresh failures appeared in `automation-openclaw-1` logs after re-auth.
- `npm view openclaw version` returned `2026.3.24`
- Dev deploy validation:
  - `automation-dev-openclaw-1` recreated successfully
  - `openclaw --version` in dev returned `OpenClaw 2026.3.24 (cff6dc9)`
  - `automation-dev-memory-api-1` returned to healthy
- Production deploy validation:
  - `automation-openclaw-1` recreated successfully
  - `openclaw --version` in production returned `OpenClaw 2026.3.24 (cff6dc9)`
  - `./scripts/vm-safe.sh --yes health` passed after rollout
  - live production agent turn still completed successfully after the upgrade

## Backups Taken
- Config backup: yes
  - `/home/node/.openclaw/config.json.pre-codex-auth-20260327-163405.bak`
- DB volume backup: no

## Known Risks / Notes
- Current production Openclaw Codex OAuth expiry is `2026-04-06T16:44:11Z`.
- `openclaw models auth login --provider openai-codex` now works in the current production container version and is preferable to a full onboarding pass for this specific repair.
- `openclaw` auth/login still rewrites `config.json`, so a backup should remain mandatory before future re-auth attempts.
- Dev deploy surfaced a transient Docker container-name conflict (`Dead` legacy containers blocking `memory-api`/`openclaw` recreation). Removing the stale dev-only containers and re-running `deploy-dev` resolved it.
- Secrets rotated (if exposed): no

## Rollback / Recovery
- If the new auth profile proves bad:
  - restore `/home/node/.openclaw/config.json.pre-codex-auth-20260327-163405.bak` over `/home/node/.openclaw/config.json`
  - compare against `/home/node/.openclaw/agents/main/agent/auth-profiles.json`
  - restart or reload Openclaw if needed
- If `2026.3.24` causes regressions:
  - revert the Dockerfile pin to `2026.3.8`
  - push and redeploy dev/prod through the same GitOps scripts
- First place to look:
  - `docker logs --since 15m --tail 200 automation-openclaw-1`
  - `openclaw models status --json`

## Next Session Plan
1. Add the minimal Codex re-auth command and the dev stale-container recovery note to [`ops/runbooks.md`](/Users/sgkmeyer/ai-automation-stack/ops/runbooks.md).
2. Continue the active backlog from [`ops/today.md`](/Users/sgkmeyer/ai-automation-stack/ops/today.md), especially mutation rollback coverage and transcript recall quality.
3. Consider adding an operator reminder for Codex OAuth expiry before the next likely expiration window.
