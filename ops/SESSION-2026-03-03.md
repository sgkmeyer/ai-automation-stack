# Session Log — 2026-03-03

## Scope
- Openclaw v2026.3.1 upgrade (dev-first validated, production deployed)
- Production smoke test fixes (retry logic, expected status codes)
- Security hardening: gateway auth rate limiting, hooks.defaultSessionKey, security audit baseline
- Dev Openclaw: fixed controlUi.allowedOrigins and config permissions

## What Changed
### 1) Infrastructure / Runtime
- **Openclaw version bump**: `2026.2.26` → `2026.3.1` in Dockerfile
  - Files: `automation/openclaw-image/Dockerfile`
- **Prod smoke test updated**: Changed expected status for openclaw.satoic.com from 401 → 200 (basic_auth was removed last session). Added 30s startup delay + 3 retries with 10s between attempts.
  - Files: `.github/workflows/deploy-prod.yml`
- **Caddy reload in gitops-deploy.sh**: Already had `caddy reload` line from previous work — confirmed working.

### 2) Security Hardening (VM config.json — gitignored)
- **gateway.auth.rateLimit** applied to both dev and production:
  ```json
  { "maxAttempts": 10, "windowMs": 60000, "lockoutMs": 300000, "exemptLoopback": true }
  ```
  - Initial attempt with wrong keys (`enabled`, `window`, `maxFailures`) failed — TAR identified the correct schema from docs (`maxAttempts`, `windowMs`, `lockoutMs`, `exemptLoopback`)
- **hooks.defaultSessionKey: "main"** applied to both dev and production — scopes webhook ingress to main session
- **Dev controlUi.allowedOrigins** added: `["http://100.82.169.113:18789", "http://localhost:18789"]`
- **Dev config.json permissions** fixed: `chmod 600`
- **Security audit baseline** saved: `ops/security-audit-2026-03-03.md`
  - Production: 0 critical, 1 warn, 3 info
  - Dev: was 2 critical → fixed to 0 critical

### 3) Operations / Docs
- Updated `ops/today.md` with version, security hardening, and audit baseline
- Created `ops/security-audit-2026-03-03.md`

## Deployment Notes
- Deploy method: GitOps (push to main → GitHub Actions → SSH → gitops-deploy.sh)
- Production environment approval gate approved via `gh api` (3 times this session)
- Smoke test failed twice due to: (1) expected 401 instead of 200, (2) Openclaw not ready in time
- Both issues fixed: updated expected code + added retry logic with startup delay
- Security config changes applied directly on VM via SSH (gitignored files)

## Validation Checks Passed
- Openclaw v2026.3.1 running on both dev and production
- All 11 production services healthy
- `https://openclaw.satoic.com/` → 200
- Full CI/CD pipeline green (deploy + smoke test with retry)
- `openclaw security audit --deep`: 0 critical on both environments
- Rate limiting config accepted without errors
- hooks.defaultSessionKey accepted without errors

## Backups Taken
- None (version bump + config changes, easily reversible)

## Known Risks / Notes
- **gateway.auth.rateLimit schema** — correct keys are `maxAttempts/windowMs/lockoutMs/exemptLoopback` (not `enabled/window/maxFailures`)
- **Smoke test timing** — Openclaw needs ~30-60s after rebuild; retry logic now handles this
- **Tailscale architecture** — decided to keep current setup (public Caddy + gateway token auth); no move to tailnet-only
- **Weekly security checks** — parked for now; can revisit later
- **CDP HTTP warning** — acceptable (internal Docker network only, not exposed)

## Rollback / Recovery
- **Openclaw version**: revert Dockerfile ARG to `2026.2.26`, push, deploy
- **Security config**: remove `rateLimit` and `defaultSessionKey` from config.json on VM, restart
- **Smoke test**: revert `.github/workflows/deploy-prod.yml` via git

## Next Session Plan
1. Update `gitops-deploy.sh` to restart Caddy on Caddyfile changes
2. Fix `vm-safe.sh` (dr-backup symlink + usage text)
3. Research n8n v2 "Personal Agents" / "Workflow Agents"
4. Build MCP bridge
5. Test JS-01 end-to-end
