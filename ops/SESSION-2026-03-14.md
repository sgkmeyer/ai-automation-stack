# Session Log — 2026-03-14

## Goal

Wire Openclaw/TARS to the live content registry so saved-link questions use the registry retrieval path instead of memory recall only.

## Changes Made

- Added a registry CLI for operators and Openclaw:
  - `scripts/registry-webhook.sh`
- Added Openclaw workspace wrappers:
  - `automation/openclaw/workspace/bin/registry`
  - `automation/openclaw/workspace/bin/query-registry`
  - `automation/openclaw/workspace/bin/list-registry`
  - `automation/openclaw/workspace/bin/review-registry`
- Added registry-specific workspace guidance:
  - `automation/openclaw/workspace/REGISTRY_POLICY.md`
  - `automation/openclaw/workspace/REGISTRY_POLICY_EXAMPLES.md`
- Updated Openclaw workspace boot/instruction files:
  - `automation/openclaw/workspace/AGENTS.md`
  - `automation/openclaw/workspace/TOOLS.md`
- Updated runtime env contract for Openclaw:
  - `automation/docker-compose.yml`
  - `automation/.env.example`
- Corrected the end-user shortcut docs to match the working iPhone flow:
  - `ops/registry-user-guide.md`

## Production Runtime Work

- Added `REGISTRY_WEBHOOK_BASE_URL` and `REGISTRY_WEBHOOK_SECRET` to the production `automation/.env`
- Deployed `main` at commit `cfd5687`
- Openclaw container was recreated during deploy with the new registry env

## Validation

- Verified production Openclaw registry list wrapper:
  - `./bin/list-registry --limit 2 --sort newest`
  - returned live registry items including recent X and TikTok saves
- Verified production Openclaw registry query wrapper:
  - `./bin/query-registry "example domain" --limit 2`
  - returned processed registry items with stored summaries and links

## Result

TARS now has a real registry retrieval lane. The earlier failure mode was accurate: Openclaw had memory wrappers only and no registry tool surface or policy instructions, so TAR was blind to registry items even though the backend existed.

## Next Follow-Ups

1. Fix registry canonicalization so obvious `http`/`https` duplicates converge.
2. Add a metadata-only fallback for `403`/blocked pages instead of hard `failed` when possible.
3. Run end-user UAT through TARS using:
   - `What did I save about ...?`
   - `Show my reading inbox`
   - `Archive the first one`

## Roadmap Lock-In

The roadmap was explicitly consolidated and locked during this session.

Canonical reference:

- `ops/roadmap-priorities.md`

Agreed prioritization:

- trust layer first
- daily utility second
- coherence third
- operating-system feel last

Immediate next tranche if there is no higher-priority incident:

1. Registry capture hardening
2. Memory hygiene and deduplication
3. Openclaw capability hardening follow-up
4. State reversibility design
5. Unified recall router v1
