# Session Log - 2026-07-19

## Scope
- Started a new operational session for the automation stack.
- Ran full startup health checks against production.
- Freed disk space on the Oracle VM after the root volume reached 100%.
- Reviewed current architecture versions against available upgrades.
- Applied the first upgrade tranche and promoted OpenClaw through dev to production.
- Ran a quiet post-deploy check before closeout.

## What Changed
### 1) Infrastructure / Runtime
- Removed stale/zero-byte VM backup artifacts and restored root disk headroom.
- Hardened `scripts/vm-cron-backup.sh`:
  - Writes config and DB archives through temporary files before promotion.
  - Validates completed archives with `tar -tzf`.
  - Prunes temporary and zero-byte backup artifacts.
  - Excludes volatile OpenClaw runtime paths:
    - `automation/openclaw/agents/*/agent/codex-home/tmp`
    - `automation/openclaw/plugin-runtime-deps`
    - `automation/openclaw/plugin-skills`
    - `automation/openclaw/npm/node_modules`
    - `automation/openclaw/npm/projects/*/node_modules`
    - `automation/openclaw/logs`
    - `automation/openclaw/tasks`
    - `automation/openclaw/delivery-queue`
- Pinned or updated operational container images:
  - Caddy: `2.11.4-alpine`
  - Portainer CE: `2.39.5-alpine`
  - Browserless Chromium base compose: `ghcr.io/browserless/chromium:v2.55.0`
  - Native Chromium override: `chromedp/headless-shell:145.0.7632.76`
  - n8n task runner: `n8nio/runners:2.11.2`

### 2) OpenClaw
- Upgraded OpenClaw from `2026.5.18` to `2026.7.1-2`.
- Deployed and smoke-tested the upgrade in the dev stack first.
- Promoted the same build to production after dev passed.
- Ran `openclaw doctor --fix --yes --non-interactive` in production.
- Doctor migrated model routing from legacy `openai-codex/gpt-5.5` to canonical `openai/gpt-5.5` while preserving the Codex harness.
- Doctor refreshed the `codex` plugin and disabled skills whose binaries or environment requirements were missing.

### 3) Operations / Docs
- Created this session closeout file.
- Documented deferred upgrade/security plan below for future sessions.

## Deployment Notes
- Deploy method used: GitOps deploy through `./scripts/vm-safe.sh --yes deploy`.
- Dev deploy used before production: `./scripts/vm-safe.sh --yes deploy-dev`.
- Production deployed branch: `main`.
- Commits promoted to production:
  - `504a541 chore: pin ops images and harden backups`
  - `f09c774 chore: bump openclaw to 2026.7.1-2`
  - `c7e9515 fix: exclude openclaw project caches from backups`

## Validation Checks Passed
- `./scripts/start-session.sh` completed with `START STATUS: CLEAN`.
- `./scripts/verify.sh` passed after disk cleanup and again after production upgrade.
- `./scripts/vm-safe.sh --yes health` passed after production upgrade and again during quiet closeout.
- External endpoint smoke checks passed:
  - `https://n8n.satoic.com` returned 200.
  - `https://openclaw.satoic.com` returned 200.
  - `https://portainer.satoic.com` returned expected 401.
- OpenClaw `/healthz` returned `{"ok":true,"status":"live"}`.
- Production OpenClaw container health: healthy.
- Production `openclaw --version` returned `OpenClaw 2026.7.1-2 (0790d9f)`.
- Production `openclaw models status --json` showed:
  - default model `openai/gpt-5.5`
  - resolved default `openai/gpt-5.5`
  - runtime auth route provider `openai`, runtime `codex`, status `usable`
- Production `openclaw memory status --json` showed builtin memory healthy:
  - files: 9
  - chunks: 28
  - dirty: false
  - FTS and vector enabled
  - no scan issues
- Live production agent smoke succeeded:
  - command: `openclaw agent --to +15555550123 --message system-health-check-ok --json`
  - result: `HEARTBEAT_OK`
  - provider: `openai`
  - model: `gpt-5.5`
  - harness: `codex`
  - tool failures: 0
- Quiet post-deploy check on 2026-07-19 around 08:56 EDT:
  - Production and dev containers were still up after about 11 hours.
  - OpenClaw and memory-api remained healthy.
  - `./scripts/vm-safe.sh --yes health` still passed.

## Backups Taken
- Backup script initially failed because live OpenClaw generated npm project caches changed during tar creation.
- Added the project-cache exclusion and reran successfully.
- Fresh production backup after final script fix:
  - Config archive: `/home/ubuntu/backups/automation-full-2026-07-18.tar.gz` (71M)
  - DB archive: `/home/ubuntu/backups/automation-db-2026-07-18.tar.gz` (27M)

## Disk Status
- Initial issue: Oracle VM root disk was full at 100%, causing n8n and memory webhook 503s and Docker exec failures with `no space left on device`.
- Main space consumers found:
  - `/home/ubuntu/backups`: about 27G
  - `/var/lib/containerd`: about 11G
  - system journal: about 1.2G
- After cleanup and final backup:
  - root filesystem: 58% used
  - free space: about 20G

## Known Risks / Notes
- Post-closeout OpenClaw Control UI login issue:
  - Symptom: browser showed "Could not connect" while connecting to the OpenClaw web interface.
  - Gateway health was OK and `/healthz` was live.
  - OpenClaw logs showed `device_token_mismatch`, then `token_missing`.
  - Fix used: generated a fresh tokenized `https://openclaw.satoic.com/#token=...` dashboard URL from the production gateway token and copied it directly to the local Mac clipboard without printing the token in chat.
  - Result: user confirmed the web login worked.
  - Likely cause: stale Control UI/device token state after the OpenClaw doctor migration.
- OpenClaw doctor still reports plaintext secret-bearing config fields:
  - `gateway.auth.token`
  - `channels.telegram.botToken`
- OpenClaw doctor warns that the gateway is bound to `lan` / `0.0.0.0`; keep auth strong and review whether loopback plus tunnel/Tailscale would be safer.
- OpenClaw doctor notes `plugins.allow` is empty while the `codex` plugin is auto-loaded; future sessions should explicitly allow trusted plugins.
- OpenClaw doctor leaves legacy `update-check.json` in place because shared SQLite state already differs.
- `openai-codex:default` remains as an expired legacy profile, but production routing uses canonical `openai` profiles and live agent smoke passes.
- n8n remains at `2.11.2`; upgrade was intentionally deferred.
- PostgreSQL remains on the 16 Alpine tag series; patch/major decisions were intentionally deferred.
- Redis remains on 7 Alpine; Redis major upgrade was intentionally deferred.
- Local working tree had a pre-existing untracked `ops/SESSION-2026-05-28.md`; this session did not modify it.

## Rollback / Recovery
- For general health regression:
  - Run `./scripts/verify.sh`.
  - Run `./scripts/vm-safe.sh --yes health`.
  - Check production compose status with `docker compose ps` on the VM.
- For OpenClaw-specific regression:
  - Check `curl -ksS https://openclaw.satoic.com/healthz`.
  - Check `docker exec automation-openclaw-1 openclaw --version`.
  - Check `docker exec automation-openclaw-1 openclaw models status --json`.
  - Check `docker exec automation-openclaw-1 openclaw memory status --json`.
  - Run a live agent smoke.
- Last known good production OpenClaw version before this session: `2026.5.18`.
- Current production OpenClaw version after this session: `2026.7.1-2`.
- Fresh config and DB backups exist under `/home/ubuntu/backups` for 2026-07-18.

## Next Session Plan
1. OpenClaw security posture:
   - Migrate plaintext OpenClaw secrets to SecretRefs.
   - Run `openclaw secrets audit --check`.
   - Run `openclaw security audit --deep`.
   - Review whether gateway bind should stay `lan` or move behind loopback plus tunnel/Tailscale.
2. OpenClaw plugin trust:
   - Inspect enabled plugins with `openclaw plugins list --enabled --verbose`.
   - Add an explicit `plugins.allow` list for trusted production plugins, including `codex`.
   - Restart OpenClaw and rerun health/model/agent smoke.
3. OpenClaw skill inventory:
   - Review doctor-disabled skills.
   - Decide which should remain disabled versus installed/enabled.
   - Avoid enabling skills that require missing binaries/env unless their operational use is clear.
4. n8n upgrade:
   - Upgrade dev stack first from `2.11.2` toward current stable.
   - Include task runner compatibility checks.
   - Smoke memory webhook and representative workflows before production.
5. PostgreSQL maintenance:
   - Patch within PostgreSQL 16 first.
   - Defer major PostgreSQL upgrade until there is a separate backup/restore rehearsal and extension compatibility check.
6. Redis maintenance:
   - Keep Redis 7 for now unless a specific feature/security driver appears.
   - Treat Redis major upgrade as a separate compatibility task.
7. Backup monitoring:
   - Recheck the next scheduled cron backup after this session.
   - Confirm config and DB archives are nonzero and `tar -tzf` valid.
   - Watch disk use under `/home/ubuntu/backups` to confirm retention pruning holds.
