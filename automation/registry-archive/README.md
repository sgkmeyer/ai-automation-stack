Registry source archives live here at runtime.

This directory is mounted into `memory-api` at `/var/lib/registry-archive`.
V1 stores raw extracted source material here for link-rot resilience, but does
not index it directly for query.
